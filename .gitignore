# 1. Firebase 비밀 키 (보안!)
# 이 파일이 유출되면 다른 사람이 내 DB에 접근할 수 있습니다.
google-service.json

# 2. 빌드 결과물 및 캐시 (용량이 크고 불필요함)
.gradle/
build/
/build/
/app/build/
captures/
.externalNativeBuild/
.cxx/

# 3. 로컬 환경 설정 (개인 PC마다 다름)
local.properties
.DS_Store

# 4. IDE (Android Studio / IntelliJ) 설정
.idea/
*.iml
*.hprof
*.ap_

# 5. 서명 키 (앱 출시용 비밀 키)
*.jks
*.keystore